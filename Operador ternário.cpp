#include <bits/stdc++.h>

using namespace std;

int main() {

    int A, B;
    printf("A = "); scanf("%d", &A);
    printf("B = "); scanf("%d", &B);

    A == B ? printf("\nA = B\n") : printf("\nA != B\n");
    printf("%s", A == B ? "A = B" : "A != B");

    return 0;
}